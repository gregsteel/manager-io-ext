# Receipt Submission — Specification

**Moved.** This specification now lives at
[`specs/003-receipt-submission/spec.md`](../../specs/003-receipt-submission/spec.md)
in the repository root, alongside the specs for the rest of the accounting stack.

It is the same document — user stories and requirements were added above the
original numbered sections (§1–§12), which are preserved in full. Section
references from elsewhere in this repo (`SPEC.md §6.2`, `§3.3`, and so on) still
resolve there.

Update that file, not this one, whenever behaviour, architecture, auth, APIs,
data model, or configuration change.

Paths inside the spec (`src/`, `ios/`, `docs/`) remain relative to
`receipt-submission/`.
