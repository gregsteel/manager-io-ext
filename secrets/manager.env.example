# Placeholder — chrborg/manager.io has no documented env-based secrets (only
# the /data volume is documented on Docker Hub). The Manager API token is
# generated inside the Manager UI itself (spec §6 step 7), not set here.
# Kept for layout parity with the spec (§8) in case a future image version
# adds env-configurable secrets.
